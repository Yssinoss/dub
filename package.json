{
  "name": "dub-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm install --ignore-scripts && turbo build",
    "dev": "turbo dev"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@11.22.0"
}
